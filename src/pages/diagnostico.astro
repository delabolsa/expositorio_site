---
import artistasData from "../data/artistas.json";
---

<html>
  <body>
    <h1>Diagnóstico con JSON</h1>
    <p>Artistas encontrados: {artistasData.length}</p>
    <ul>
      {artistasData.map(a => <li>{a.slug} - {a.data.nombre}</li>)}
    </ul>
  </body>
</html>
